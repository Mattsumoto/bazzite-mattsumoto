# RTX 3080 (Ampere) tuning for the open kernel modules.
# Mirrors the kargs so the settings apply even if initramfs loads the module early.

options nvidia NVreg_PreserveVideoMemoryAllocations=1 NVreg_TemporaryFilePath=/var/tmp
options nvidia_drm modeset=1 fbdev=1
